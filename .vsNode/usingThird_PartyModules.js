const _= required('lodash')//this kind of search will result in a search inside moodules and the index archive
setInterval(() => console.log(_.random(1,1000)),2000) //will repeat every 2000 milseconds a random number between 1 and 1000
